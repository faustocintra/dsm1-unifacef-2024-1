import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      padding: 10,
      margin: 10,
      borderRadius: 10,
      borderWidth: 2,
    },
    texto: {
      fontSize: 16,
      marginBottom: 5,
    },
    verde: {
      borderColor: 'green',
    },
    laranja: {
      borderColor: 'orange',
    },
  });

export default styles