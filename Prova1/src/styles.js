import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#F5F5F5', // Cor de fundo diferente de branco
    padding: 12, // Espaçamento interno de 12
  },
  title: {
    fontSize: 25, // Tamanho da fonte 25
    color: 'blue', // Cor diferente de preto
  },
  label: {
    fontStyle: 'italic', // Fonte em itálico
  },
  resultText: {
    fontStyle: 'italic', // Fonte em itálico
  },
  buttonContainer: {
    marginVertical: 10, // Adicionando margem vertical para o botão
  },
});

export default styles;
