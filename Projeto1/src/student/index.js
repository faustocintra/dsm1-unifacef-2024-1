import React from "react";
import { Text, View } from "react-native";
import { styles } from "./style";

const Student = ({ title, children }) => {
    return (
        <View>
        </View>
    )
}

export default Student