import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    textInput:{
        borderColor: '#888',
        borderWidth: 1
    },
    horizontal: {
        flexDirection: 'row'
    }
})